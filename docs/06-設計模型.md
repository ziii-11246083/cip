# 第 6 章 設計模型

> 本章包含系統設計模型的完整說明，涵蓋分析類別圖（問題域）、設計類別圖（解決方案域）、循序圖、系統分層架構圖。

## 6-1 分析類別圖 vs 設計類別圖

### 6-1-1 為什麼需要兩種圖？

| | 分析類別圖 (Analysis Class Diagram) | 設計類別圖 (Design Class Diagram) |
|---|---|---|
| **關注點** | 問題域（What） | 解決方案域（How） |
| **抽象層級** | 高層次，與技術無關 | 低層次，包含框架與實作細節 |
| **對象** | 領域專家、需求分析師 | 開發者、架構師 |
| **內容** | 邊界類別、控制類別、實體類別 | 具體類別、方法簽章、框架繼承 |
| **技術細節** | 無（不指定語言/框架） | 有（Python/Flask/Supabase） |

**在此專案中的分工**：
- 分析類別圖：描述「加密貨幣投資者需要哪些概念」（如 Account、Portfolio、RiskReport），與技術無關
- 設計類別圖：描述「Flask 如何實現這些概念」（如 Config、SupabaseDB、FlaskApp、DataManager），包含具體方法與依賴關係

### 6-1-2 UML Visibility 標記說明

| 標記 | 名稱 | 意義 | Python 對應 |
|------|------|------|-------------|
| `+` | Public | 任何類別皆可存取 | 一般方法/屬性 |
| `-` | Private | 僅該類別內部可存取 | `__method` / `_method` |
| `#` | Protected | 該類別與子類別可存取 | `_method`（慣例） |
| `~` | Package | 同一套件內可存取 | 較少使用 |

---

## 6-2 分析類別圖（問題域）

以下為系統的問題域模型，不含技術實作細節。

```mermaid
classDiagram
    class 投資者 {
        +電子郵件
        +顯示名稱
        +註冊日期
        +訂閱方案
        +登入()
        +管理訂閱()
    }

    class 投資組合 {
        +總預算
        +現金餘額
        +建立日期
        +加入資產()
        +計算市值()
        +查詢權益曲線()
    }

    class 加密貨幣 {
        +代號
        +名稱
        +中文名稱
        +市值排名
        +當前價格()
        +歷史價格()
    }

    class 風險報告 {
        +風險分數
        +集中度
        +波動率
        +最大回撤
        +產生報告()
        +匯出()
    }

    class 交易 {
        +方向
        +數量
        +成交價
        +金額
        +時間戳
    }

    class 社群情緒 {
        +來源
        +情緒分數
        +提及次數
        +正面/負面比率
    }

    class 詐騙檢測 {
        +合約地址
        +風險等級
        +檢測原因
        +執行掃描()
    }

    class 對話 {
        +標題
        +建立時間
        +發送訊息()
        +查詢歷史()
    }

    投資者 "1" --> "*" 投資組合 : 擁有
    投資者 "1" --> "*" 對話 : 發起
    投資組合 "1" --> "*" 加密貨幣 : 持有
    投資組合 "1" --> "*" 交易 : 記錄
    加密貨幣 "1" --> "*" 風險報告 : 產生
    加密貨幣 "1" --> "*" 社群情緒 : 分析
    加密貨幣 "1" --> "*" 詐騙檢測 : 掃描
```

---

## 6-3 設計類別圖（解決方案域）

以下為系統的實作層級設計類別圖，對應 `app.py` + `supabase_client.py` 的實際結構。

```mermaid
classDiagram
    class Config {
        +CG_API_KEY: str
        +OPENAI_API_KEY: str
        +CACHE_TTL: int
        +MARKET_COIN_LIMIT: int
        +SFI_COIN_LIMIT: int
        +COIN_META: dict
        +RSS_FEEDS: list
    }

    class SupabaseDB {
        -url: str
        -key: str
        -client: Client
        -available: bool
        +create_user(email, password, username) dict
        +get_user_profile(user_id) dict
        +update_user_preferences(user_id, prefs) bool
        +upsert_cryptocurrencies(coins) dict
        +insert_price_data(symbol, data) bool
        +save_crypto_report(symbol, data) bool
        +save_sentiment_analysis(symbol, data) bool
        +log_user_activity(user_id, data) bool
        +add_to_watchlist(user_id, symbol, name) bool
        +sim_get_or_create_portfolio(token, cash) dict
        +sim_execute_order(token, ...) dict
        +sim_list_positions(token) list
        +sim_list_transactions(token, limit) list
        +sim_list_equity_curve(token, limit) list
        +create_conversation(user_id, title, model) str
        +save_message(conversation_id, ...) bool
        +get_conversation_history(conversation_id) list
        +list_conversations(user_id) list
    }

    class DataManager {
        +get_all_tickers() list
        +get_market_tickers() list
        +build_historical_df(tickers) DataFrame
        +search_sfi_assets(query) list
        -_cg_get(endpoint, params) dict
    }

    class RiskModel {
        +calculate_copula_risk(symbol, df, is_stable, price) dict$
        -_fit_copula_correlation(returns_x, returns_y) tuple$
    }

    class SocialMediaEngine {
        +scrape_ptt() list$
        +scrape_rss_for_signals() list$
        +fetch_narratives_full() dict$
        +scan_scam(address) dict$
    }

    class FlaskApp {
        +home()
        +market_page()
        +api_market()
        +api_ai_chat()
        +sim_trade_page()
        +podcast_page()
        +scam_detect_page()
        +health_page()
    }

    class SimulationStore {
        +load_local_sim_store() dict
        +save_local_sim_store(store)
        +default_local_sim_state(key, cash) dict
        +local_execute_sim_order(...) dict
        +get_local_sim_state(token, cash) tuple
    }

    class UserProfile {
        +uid: str
        +email: str
        +display_name: str
    }

    Config <|-- DataManager : 使用
    Config <|-- SocialMediaEngine : 使用
    SupabaseDB --> FlaskApp : 注入
    DataManager --> FlaskApp : 調用
    RiskModel --> FlaskApp : 調用
    SocialMediaEngine --> FlaskApp : 調用
    SimulationStore --> FlaskApp : 備援
    SupabaseDB --> UserProfile : 讀寫
    FlaskApp --> OpenAI : 外部API
    FlaskApp --> CoinGecko : 外部API
    FlaskApp --> yfinance : 外部API
```

---

## 6-4 系統分層架構圖

系統採用 **四層架構（Layered Architecture）**：

```mermaid
flowchart TB
    subgraph Presentation["Presentation Layer 展示層"]
        HTML["HTML Templates<br/>16 個頁面"]
        CSS["CSS Stylesheets<br/>11 個樣式檔"]
        JS["JavaScript Modules<br/>15 支腳本"]
    end

    subgraph Application["Application Layer 應用層"]
        Flask["Flask Routes<br/>45 條路由"]
        Auth["Auth Decorators<br/>token_required / optional_token"]
        Cache["TTL Cache<br/>@ttl_cache"]
    end

    subgraph Domain["Domain Layer 領域層"]
        DM["DataManager<br/>市場數據聚合"]
        RM["RiskModel<br/>SFI 風險計算"]
        SME["SocialMediaEngine<br/>社群爬蟲 & 詐騙檢測"]
        PM["PortfolioManager<br/>模擬交易邏輯"]
        AIS["AI System Prompt<br/>系統提示建構"]
    end

    subgraph Infrastructure["Infrastructure Layer 基礎設施層"]
        Supabase["Supabase<br/>Auth / DB / RPC"]
        OpenAI["OpenAI API<br/>GPT-4o-mini / TTS"]
        CG["CoinGecko API<br/>即時市場數據"]
        YF["yfinance<br/>歷史價格"]
        GMGN["GMGN API<br/>合約安全掃描"]
        LocalFS["Local File System<br/>模擬交易備援"]
    end

    Presentation --> Application
    Application --> Domain
    Domain --> Infrastructure
```

---

## 6-5 關鍵循序圖

### 6-5-1 AI 投資教練對話流程

```mermaid
sequenceDiagram
    participant U as 使用者
    participant B as 瀏覽器
    participant F as Flask
    participant DB as Supabase
    participant AI as OpenAI

    U->>B: 輸入問題
    B->>B: 檢查登入狀態
    alt 未登入
        B->>U: 顯示會員鎖定遮罩
    else 已登入
        B->>F: POST /api/ai-chat (Bearer Token)
        F->>F: @token_required 驗證
        F->>DB: 取得對話歷史 (ai_messages)
        DB-->>F: 歷史訊息
        F->>F: build_ai_system_prompt(risk_profile)
        F->>AI: Chat Completion (system + history + user_msg + tools)
        AI-->>F: AI 回應 (可能含 tool_calls)
        opt AI 呼叫 get_crypto_price_usd
            F->>F: 查詢即時幣價
            F->>AI: 回填 tool result
            AI-->>F: 最終回應
        end
        F->>DB: 儲存訊息 (ai_messages)
        F-->>B: { reply, conversation_id }
        B->>U: 顯示回應
    end
```

### 6-5-2 模擬交易下單流程

```mermaid
sequenceDiagram
    participant U as 使用者
    participant B as 瀏覽器
    participant F as Flask
    participant DB as Supabase
    participant CG as CoinGecko

    U->>B: 輸入幣種/方向/數量
    B->>F: POST /api/sim-trade/order (Bearer Token)
    F->>F: @token_required 驗證
    F->>CG: 查詢即時價格
    CG-->>F: 當前報價
    F->>F: 計算交易後總資產
    alt Supabase 可用
        F->>DB: RPC sim_execute_order()
        DB-->>F: 交易結果
    else Supabase 不可用
        F->>F: local_execute_sim_order() 備援
    end
    F-->>B: { cash, positions, trade }
    B->>U: 更新 dashboard
```

### 6-5-3 詐騙檢測流程

```mermaid
sequenceDiagram
    participant U as 使用者
    participant B as 瀏覽器
    participant F as Flask
    participant GMGN as GMGN API
    participant PTT as PTT 爬蟲

    U->>B: 輸入合約地址
    B->>F: POST /api/scam-scan
    F->>GMGN: 合約安全掃描
    GMGN-->>F: 合約風險報告
    F->>PTT: 搜尋相關討論
    PTT-->>F: 社群討論摘要
    F->>F: 綜合評分 (合約+社群)
    F-->>B: { risk_level, reasons, warnings }
    B->>U: 顯示檢測結果
```

---

## 6-6 元件圖（Component Diagram）

```mermaid
graph TB
    subgraph "Smart Invest Crypto Platform"
        subgraph "Frontend"
            FE_Templates["Templates (Jinja2)"]
            FE_CSS["CSS"]
            FE_JS["JavaScript (ES Modules)"]
        end

        subgraph "Backend"
            BE_App["Flask Application"]
            BE_Auth["Auth Middleware"]
            BE_Cache["Cache Layer"]
        end

        subgraph "Domain Logic"
            DL_Market["Market Data"]
            DL_Risk["Risk Engine"]
            DL_Social["Social Media Engine"]
            DL_AI["AI Orchestrator"]
            DL_Sim["Simulation Engine"]
        end

        subgraph "External Services"
            EXT_SB["Supabase (Auth/DB)"]
            EXT_OAI["OpenAI (Chat/TTS)"]
            EXT_CG["CoinGecko"]
            EXT_YF["Yahoo Finance"]
            EXT_GMGN["GMGN Security"]
        end
    end

    FE_Templates --> BE_App
    FE_JS --> BE_App
    BE_App --> BE_Auth
    BE_App --> BE_Cache
    BE_App --> DL_Market
    BE_App --> DL_Risk
    BE_App --> DL_Social
    BE_App --> DL_AI
    BE_App --> DL_Sim
    DL_Market --> EXT_CG
    DL_Market --> EXT_YF
    DL_Social --> EXT_GMGN
    DL_AI --> EXT_OAI
    DL_Sim --> EXT_SB
    BE_Auth --> EXT_SB
```

---

> 本章所有 Mermaid 圖表可在支援 Mermaid 的 Markdown 編輯器中直接渲染，亦可用 `scripts/markdown_to_image.py` 轉換為 PNG。
