# 第 19 章 複評證據包

> 基準日期：2026-08-20。此文件只整理可重現證據；不把本機未提交內容說成 GitHub 已存在，也不把 migration-ready、Beta、planned 或 pending human review 說成已上線。

## 19-1 展示前先說清楚的四個狀態

| 狀態 | 可以說 | 不可以說 |
|---|---|---|
| GitHub checkpoint | `origin/08/20` 已有 `5b7f523` | Task 05A 後續全部已推上 GitHub |
| 本機 verified worktree | pytest 284/284 與三組 validator 已通過 | 線上環境、正式 DB 或 provider 已驗證 |
| code / migration ready | 有 versioned SQL、RLS、service 與 fake integration tests | migration 已執行、真實付費會員已使用 |
| planned / pending | 有資料契約或商業假設 | 是產品已完成、已有營收或準確率 |

## 19-2 GitHub 與版本證據

Repository：[ziii-11246083/cip](https://github.com/ziii-11246083/cip)

### 19-2-1 分支現況

| Ref | Commit | 判定 |
|---|---|---|
| `origin/08/20` | `5b7f52351c92e75406e9d8296bf103b8364423ac` | 本次複評 checkpoint；本機 branch 與 remote ref 已核對一致 |
| `origin/0727` | `ca7fa7d` | 7/27 Demo Auth / navigation 整理的歷史分支 |
| `origin/0710-RAG-plus` | `0d14ba0` | RAG Phase 2A 歷史分支 |
| `origin/0709` | `972adcd` | 7/9 整合版歷史分支 |
| `origin/main` | `4e3a8aa` | 舊且與複評線分歧；**不應拿來代表目前複評版本** |

現階段不重寫 branch 歷史、不 force-push。若要把 `08/20` 設為正式主線，應在全部變更提交並經測試後另開 PR／由 repository owner 明確決定，不在文件任務中偷改 default branch。

### 19-2-2 重要 commits（以成果，不以數量評價）

| Commit | 可驗證內容 | 代表證據 |
|---|---|---|
| `bde22c3` / `fe089a5` | 初始專案與根目錄整理 | Flask、templates、static、Supabase client、Procfile 的 repository bootstrap |
| `f29fc9c` | 模擬交易改接 Supabase 介面 | `app.py`, `supabase_client.py`, member/agent 前端 |
| `2909796` | Portfolio health API / 共用風險計算 | `app.py` |
| `893da7f` | SFI 與相關 UI 修正 | `app.py`, analysis / market / UI files |
| `0d14ba0` | RAG Phase 2A hybrid retrieval | `services/*rag*`, BM25/vector/rerank、knowledge、eval/docs |
| `ca7fa7d` | Demo auth 與 navigation responsiveness | auth/common/member/podcast/sim pages |
| `5b7f523` | RAG trace、citation、feedback 與 05A foundation checkpoint | migration、trace service、eval、tests、task/review evidence |

### 19-2-3 尚未推送的證據邊界

`5b7f523` 之後的 Task 05A 修正、05B、06–11 與本 Task 12 文件目前仍在本機 worktree。這些內容已由本機測試驗證，但在下一次明確 commit/push 前，評審只能看本機 demo／diff，不應說 GitHub remote 已包含。

## 19-3 成員貢獻與 90 秒答題卡

Git 只能證明 author identity、commit 與檔案，不能證明真實姓名、工時或所有內容的原始作者。以下先用 repository 內五個 human author identities；正式報告前由團隊把 alias 對回名冊。`Codex <codex@local>` 是工具紀錄，不列為成員。

| Git identity | 可定位貢獻 | 90 秒回答重點 | 現場證據 |
|---|---|---|---|
| `ziii-11246083` | 初始 repository/bootstrap、Flask/前端/Supabase client/部署檔整合；多次目錄整理 | 先說明產品問題、整體頁面與部署骨架；誠實說早期為整包上傳，不能用檔案數主張個人獨作 | `bde22c3`, `fe089a5`, `Procfile`, app entry |
| `harry02031225` | Auth/註冊、AI conversation、模擬交易 Supabase、market/health APIs、PTT resilience、UI bug fixes | 聚焦 JWT/user isolation、sim 資料流、market/health fallback；能說清楚什麼寫 DB、什麼只是 local fallback | `f29fc9c`, `bf87e0e`, `2909796`, `882b823` |
| `sumi7728` | SFI 修正、Podcast/前端整合，以及一次含 RAG services/knowledge 的整合上傳 | 聚焦 SFI 風險分層、Podcast/介面與 knowledge files；因部分 commit 是 upload，正式口試前確認哪些段落是本人設計 | `893da7f`, `9a65efe`, `e515abf` |
| `yuuuuhy` | social sentiment HTML 結構、market/member/podcast CSS 修正 | 聚焦可用性、跨頁視覺一致與 HTML 結構；展示具體 before/after commit，而不是用行數 | `077f1d3`, `9e9bfea`, `7a9b0bd`, `6539837` |
| `Brain` | 7/9 整合、RAG Phase 2A、Demo auth；8/20 trace/eval checkpoint 與複評任務證據 | 聚焦 Router→Rewrite→Hybrid Retrieval→Rerank→trace/eval；主動說 15 題仍 pending human review、remote 只到 checkpoint | `972adcd`, `0d14ba0`, `ca7fa7d`, `5b7f523` |

每位成員的統一回答結構：`我負責的使用者問題（20 秒）→ 實際資料流（30 秒）→ 一個失敗情境與防護（20 秒）→ 一個可點開的 commit/test/demo（20 秒）`。

## 19-4 評審建議 → 修改 → 證據 → Demo

狀態定義與完整文字以 `docs/10-評審建議修正對照表.md` 為準。

| ID | 狀態 | 修改／回答 | 程式或文件證據 | Demo 步驟 |
|---|---|---|---|---|
| A1 | implemented evidence | 依真實 model/env 重算成本並標來源/日期/待確認 | `docs/05` §5-2 | 開成本公式與三情境表，現場替換 token 數重算 |
| A2 | implemented evidence | 定價明標假設；列單位貢獻與損平敏感度 | `docs/05` §5-3 | 調整固定成本列，說明 80/20 mix 的 break-even |
| A3 | implemented evidence | 官方來源比較 TradingView/CoinStats/Koinly/CoinGecko | `docs/05` §5-4 | 點官方連結，說一項競品更強與一項自身定位 |
| A4 | implemented evidence | implemented/partial/planned 分層 | `docs/18` | 找 subscription、sim、wallet 三列對照 |
| A5 | implemented | 共用 footer 免責 | `templates/partials/footer.html` | 開任兩個 production pages 看相同 footer |
| B1 | implemented evidence | 分析模型與設計模型分開 | `docs/06` §6-2/6-3 | 同時展示兩張圖的技術細節差異 |
| B2 | implemented evidence | 說明 UML visibility | `docs/06` §6-2-3 | 指出 `+/-/#` 圖例 |
| B3 | implemented evidence | 補實際與 planned 流程圖 | `docs/13` | 展示 sim、asset sync、stress 三條不同資料流 |
| B4 | implemented evidence | 四層架構 | `docs/06` §6-4 | 從 route 指到 domain/service 與 infrastructure |
| B5 | implemented evidence | 套件圖 | `docs/07` §7-7 | 以 RAG services 為例說依賴方向 |
| C1 | partial | 資料字典已擴充，但不是所有表都有 DDL | `docs/08`, `docs/18` | 對比 RAG/asset migrations 與 planned subscriptions |
| C2 | planned | subscription/plans/payment 只有概念 | `docs/08` §8-2-3~5 | 明說無 checkout/webhook，不做假付款 demo |
| C3 | planned | narrative/scam persistence 未實作 | `docs/08`, `docs/18` | 跑 scam response，說明不寫 scam log |
| C4 | planned | health report 只即時回傳 | `docs/18` | 執行 health，再指出無 persistence insert |
| C5 | partial | 新的公開錢包四表契約與 adapter ready；舊交易所 link planned | `docs/ASSET_SYNC_DATA_CONTRACT.md`, asset migrations | 開 member 真實資產區；若 DB 未部署只展示 disabled/fixed state |
| C6 | planned | notification table/UI/CRUD 未實作 | `docs/08`, `docs/18` | 主動說明不展示不存在的通知中心 |
| C7 | implemented evidence | 欄位型別/用途/狀態字典 | `docs/08`, two data contracts | 任選 `rag_runs` 與 `asset_snapshots` 對照 migration |
| C8 | partial | sim client/RPC + local fallback 存在，repo 缺 sim DDL/RPC | `supabase_client.py`, `docs/18` | Demo member 跑 local paper trade，說明部署缺口 |
| D1 | implemented evidence | Domain Knowledge、tool、guardrail、RAG 注入具體化 | `docs/12`, `services/prompt_builder.py` | AI Chat 展開 citation，指 actual injected source |
| D2 | implemented | SFI/Copula/Beta/Lambda 程式與文件 | `app.py`, `docs/12` §12-3 | 開單幣分析，說輸入/公式/輸出與限制 |
| D3 | implemented | scam = deterministic text rules + local RAG + LLM | `app.py`, scam UI/tests | 輸入「保證獲利＋助記詞」，展示 reasons/evidence/uncertainty |
| D4 | implemented | Agent I→P→O 與固定風控 | `app.py`, `docs/12` §12-5 | 同一 goal 改 budget/profile，比較配置與 trace snapshot |
| D5 | implemented mechanism | trace + feedback + 15-case runner + regression contract | trace migration/service、`scripts/eval_rag.py` | 展示 artifact schema；明說未有 approved baseline |
| D6 | implemented method / no historical engine | 有回測方法文件；runtime 是 synthetic stress test | `docs/12`, `paper_stress_service.py` | 同 seed 重跑四情境，結果一致且 ledger 不變 |
| E1 | implemented | 三策略固定風險權重 | stress service/UI/tests | 切 conservative/balanced/aggressive 比較同一 snapshot |
| E2 | implemented | normal/bull/bear/black_swan 五 metrics | 同上 | 顯示四列結果與 synthetic period/seed |
| E3 | partial | Alchemy Ethereum 公開地址唯讀 MVP；無 exchange/scheduler | asset service/migrations/member UI | 只展示公開地址、不輸入任何 secret；未部署則展示契約與 test |
| E4 | partial | 社群 parse/摘要/啟發式處理存在；完整去重/窗口/determinism 未完成 | `app.py`, `docs/07`, `docs/18` | 展示頁面後主動說 PTT 與隨機 sentiment 限制 |
| E5 | implemented | 全站免責落點 | shared footer | 同 A5 |
| F1 | implemented evidence | 建立功能/route/service/table/auth/status 矩陣 | `docs/18` | 由任一 UI 追到 route 與 table/status |
| F2 | implemented evidence | 更新口試答案、成員卡與 demo 路徑 | `docs/11`, 本文件 | 每位成員依 90 秒結構演練 |
| G1 | implemented mechanism / quality pending | RAG trace、offline metrics、baseline gate；不宣稱準確率 | RAG contract/eval/tests | 跑 evaluator或展示 smoke artifact，說明 pending human review |
| G2 | partial | 公開地址自動抓資產能力已做 code MVP；付費/交易所/排程未做 | asset sync contract/service/UI | 以 feature flag 與 last-good/partial/error 狀態說安全邊界 |

## 19-5 建議 8 分鐘複評 Demo

1. 45 秒：開 `docs/18` 說 implemented/partial/planned 原則。
2. 60 秒：AI Chat 問風險問題，展開 citations，送一次 feedback；說 feedback 不是 ground truth。
3. 60 秒：開 eval artifact/README，說 15 題、metrics、baseline approval；明說仍 pending human review。
4. 60 秒：Scam 輸入「保證每天 10% 並索取助記詞」，展示 deterministic high floor 與未做外部掃描警語。
5. 75 秒：Member 真實資產區說 Ethereum 公開地址、唯讀、no secret、last-good；若 migration 未部署，不做假成功畫面。
6. 75 秒：Sim Trade 建立 snapshot，跑四情境與三策略；重跑同 seed，說不是歷史回測且 ledger 不變。
7. 60 秒：開 `docs/05` 成本公式與競品表，現場算 base break-even；說定價與 Stripe 費率仍待市場／台灣商戶驗證。
8. 45 秒：開 GitHub `08/20` 的 `5b7f523`，再說後續是本機 verified/unpushed，避免版本誤導。

## 19-6 答辯紅線

- 不說「RAG 很準」；說「已能量測與防 regression，人工 baseline 尚未核准」。
- 不說「自動同步交易所」；說「Ethereum 公開地址唯讀 Beta code ready，未部署 DB、無 scheduler」。
- 不說「詐騙掃描合約／網域／鏈上」；說「可疑文案風險辨識」。
- 不說「已有付費會員／營收」；說「商業模型可重算，金流與 entitlement planned」。
- 不說「有歷史回測」；說「即時 paper trading + 獨立 synthetic stress test」。
- 不說「GitHub 已有全部修正」；除非後續真的把本機 changes commit/push 並核對 remote ref。
